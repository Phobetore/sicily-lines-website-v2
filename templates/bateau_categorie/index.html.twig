{% extends 'base.html.twig' %}

{% block title %}BateauCategorie index{% endblock %}

{% block body %}
    <h1>BateauCategorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NbMax</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bateau_categorie in bateau_categories %}
            <tr>
                <td>{{ bateau_categorie.id }}</td>
                <td>{{ bateau_categorie.nbMax }}</td>
                <td>
                    <a href="{{ path('app_bateau_categorie_show', {'id': bateau_categorie.id}) }}">show</a>
                    <a href="{{ path('app_bateau_categorie_edit', {'id': bateau_categorie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_bateau_categorie_new') }}">Create new</a>
{% endblock %}
