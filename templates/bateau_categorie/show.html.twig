{% extends 'base.html.twig' %}

{% block title %}BateauCategorie{% endblock %}

{% block body %}
    <h1>BateauCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bateau_categorie.id }}</td>
            </tr>
            <tr>
                <th>NbMax</th>
                <td>{{ bateau_categorie.nbMax }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bateau_categorie_index') }}">back to list</a>

    <a href="{{ path('app_bateau_categorie_edit', {'id': bateau_categorie.id}) }}">edit</a>

    {{ include('bateau_categorie/_delete_form.html.twig') }}
{% endblock %}
